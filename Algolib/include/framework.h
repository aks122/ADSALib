#ifndef FRAMEWORK_H
#define FRAMEWORK_H
#define _LOG_(ELEM, x) if (ELEM != nullptr) \
*ELEM << x << '\n' \

#include <functional>
#include <cstddef>
#include <string>
#include <map>
#include <vector>
#include <algorithm>
#endif